#Contrib Folder

The source files in this folder are external libraries from other Open Source projects, that eventually I'll merge the code into the main UPS.py script.
For now the current code still relies directly on these files. 
